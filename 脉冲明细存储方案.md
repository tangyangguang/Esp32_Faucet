# 脉冲明细存储方案

## 目标

脉冲明细只服务于后续生成校准参数，不作为普通出水历史的长期日志。存储策略需要同时满足：

- 正常出水不频繁写入大块 Flash。
- 最近出水仍可查看脉冲明细，并可对最后一条记录确认实际容量。
- 已确认容量的样本自动进入 Flash 样本库，供后续生成校准参数。
- RAM 和 Flash 的单条明细上限一致，避免 RAM 可显示但无法保存的状态。

## RAM 最近明细

RAM 只保存最近 N 条脉冲明细。

- N 由系统参数“RAM 最近脉冲明细条数”控制。
- 默认值为 10。
- 可配置范围为 1 到 10。
- 超过 N 条时删除最旧明细。
- 每条明细最多保存 1024 个秒级数据点，约 4KB。

单次出水超过 1024 秒后，本次明细停止追加并标记为“已截断”。出水记录、总脉冲、统计和安全控制仍继续按正常流程完成。

## Flash 校准样本库

Flash 只保存校准样本，不保存普通出水的全部脉冲明细。

- 用户确认或校准最后一条出水记录的实际容量后，系统自动尝试把对应 RAM 明细保存到 Flash。
- 不再提供独立“保存到设备”按钮。
- 已保存样本仍允许用户主动删除。
- Flash 样本库继续使用固定槽位文件结构，单条样本最多 1024 个秒级数据点。

以下明细不进入 Flash 样本库：

- RAM 中已被淘汰的明细。
- 样本点数为 0 的明细。
- 超过 1024 秒并被标记为截断的明细。
- Flash 中已经存在同一条出水记录的样本。
- Flash 样本库已满或损坏时无法写入的明细。

自动入库失败只影响样本库，不回滚记录级实测容量保存。

## 校准参数生成

候选校准参数只扫描 Flash 样本库。

参与生成的样本必须同时满足：

- 已保存到 Flash。
- 容量已确认。
- 明细未截断。
- 稳态识别成功。

RAM 最近明细只用于查看和确认最后一条记录容量，不直接参与候选参数生成。

## 页面语义

页面不再暴露“保存到设备”动作。

- 校准页展示最近 RAM 明细和已保存 Flash 样本库状态。
- 最后一条记录确认容量后自动入库。
- 明细页展示样本状态和截断状态。
- 已保存样本保留删除按钮。
- 已截断明细明确提示“不入库，不参与生成校准参数”。

## Flash 寿命考虑

正常接水只写普通出水记录，不写大块脉冲明细。只有用户执行容量确认或校准时，才会自动写入一条约 4KB 的样本。校准是低频行为，因此该策略兼顾逻辑简化和 Flash 寿命。
