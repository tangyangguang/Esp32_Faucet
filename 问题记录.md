# Esp32_Faucet 项目问题记录

> 评估时间：2026-05-09
> 评估范围：src/、include/、test/、docs/、根目录工程文件
> 评估方法：3 个并行子代理深度探索 + 关键文件人工核实
> 立场：**仅评估，不修复**。每条问题附 1–2 句修复方向供后续派工。

---

## 0. 严重等级与编号说明

| 等级 | 含义 |
|---|---|
| 高 | 已能复现的功能/数据/安全缺陷，或测试覆盖出现关键空白 |
| 中 | 不会立即出问题，但在边界条件、长期运行、多人协作场景下会失效；或文档与实现脱节 |
| 低 | 防御性建议、风格问题、可观察项 |

编号前缀：`C`=代码核心、`W`=Web/持久化、`D`=文档、`T`=测试、`E`=工程构建。

---

## 1. 问题汇总表（按严重等级排序）

| 编号 | 严重 | 一句话描述 |
|---|---|---|
| W-01 | 高 | Web 表单 checkbox 无法关闭：取消勾选预设 `enabled` / 蜂鸣器 `beepEnabled` 后保存，旧值不变 |
| T-01 | 高 | 缺少配置版本升级路径专项测试（v1→v3、v2→v3、未知版本、损坏 NVS） |
| T-02 | 高 | WaterController 缺少硬件故障场景端到端断言（无脉冲超时、高流量持续保护、阀超时强制断电） |
| T-07 | 高 | AppController 顶层集成测试链路覆盖不足 |
| C-04 | 中 | initializeApplication 中 `new` 分配无失败检查；config 加载失败无降级日志 |
| C-06 | 中 | 工厂复位组合键松手后再按可重复触发，上层无锁定 |
| C-07 | 中 | FlowPulseReader 环形缓冲丢包仅静默累计，未上报 UI/日志 |
| W-02 | 中 | HTML 属性中输出预设/滤芯名仅依赖 writeHtmlEscaped，未确认其转义单引号 |
| W-03 | 中 | 32KB 日志 JSON 静态缓冲 + WaterLogRecord 静态数组占用显著 RAM |
| W-04 | 中 | Web 与 AppController/ConfigStore 之间的并发模型未在代码中显式表达 |
| W-05 | 中 | LittleFS 容量监控/告警缺失 |
| W-06 | 中 | NVS 中 `ver` 大于 `kConfigVersion`（如降级固件）会静默回落到默认配置 |
| D-01 | 中 | 配置版本化机制（v1→v3 迁移）在 docs/ 中无对应设计说明 |
| T-03 | 中 | FlowMeter 缺少高频脉冲、浮点边界（pulsePerMl 极小/极大）测试 |
| T-04 | 中 | CalibrationController 缺少取消重启、连续多目标校准的状态转移测试 |
| T-05 | 中 | ButtonInput 长按竞态、多键组合序列、消抖边界缺乏覆盖 |
| T-06 | 中 | DisplayPresenter 页面切换/睡眠唤醒/超时切换测试薄弱 |
| T-08 | 中 | Web 路由与 JSON 反序列化的恶意/畸形输入场景未明显覆盖 |
| E-01 | 中 | platformio.ini 含绝对路径 `/Users/tyg/...`，仅适用当前开发者机器 |
| E-02 | 中 | platformio.ini 中 webota 默认凭据 `admin/admin` 明文入库 |
| C-01 | 低 | `FlowMeter::flowFromInterval` 未做函数级 `intervalUs==0` 防御 |
| C-02 | 低 | `FlowPulseReader::droppedPulses` 读 `dropped_` 未在临界区，违反 volatile 最佳实践 |
| C-03 | 低 | `WaterController::checkTarget` 对 `preset->value==0` 无显式守卫 |
| C-05 | 低 | 全局裸指针 `g_app/g_display` 生命周期管理薄弱，无重新初始化路径 |
| C-08 | 低 | `millis()` 49 天回绕跨越点会丢失长按/超时窗口 |
| W-07 | 低 | 出水控制使用黑名单 + 子串匹配（`/api/faucet/water/`），长期不如白名单 |
| D-02 | 低 | docs/04 本地菜单（长按 NEXT）与代码实际能力对照模糊 |
| D-03 | 低 | old-docs/ 与新 docs/ 矛盾点未在每篇旧文档头部标注"已被替代" |
| D-04 | 低 | docs/06-implementation-plan.md 任务进度未更新到当前已完成状态 |
| D-05 | 低 | docs/base-library-requests/ 中各请求项未追踪"已合入/待解决"状态 |
| E-03 | 低 | .gitignore 极简，未排除 `.env*`、`build/`、`*.swp` 等常见产物 |
| E-04 | 低 | 无 CI 配置，`pio test -e native` 与构建依赖手动执行 |
| E-05 | 低 | 缺少 LICENSE 文件 |

---

## 2. 详细问题清单

### 2.1 代码 — 业务核心层

#### C-01 `FlowMeter::flowFromInterval` 缺乏函数级除零防御 — 低
- **位置**：[src/app/FlowMeter.cpp:86–96](src/app/FlowMeter.cpp:86)
- **现状**：调用方（`FlowMeter::snapshot` 行 68）已在 `interval > 0` 才会进入；理论上不会触发；`pulsePerMl_` 也由 `sanitizeConfig` clamp 到 [0.05, 5.0]。
- **隐患**：若未来调用路径变更或 `setPulsePerMl` 被旁路调用为 0，会得到 inf/NaN，传播到 UI 和日志。
- **修复方向**：函数开头加 `if (intervalUs == 0 || pulsePerMl_ <= 0.0f) return 0;`，作为深度防御。

#### C-02 `FlowPulseReader::droppedPulses()` 读取未进入临界区 — 低
- **位置**：[src/drivers/FlowPulseReader.cpp:43–45](src/drivers/FlowPulseReader.cpp:43)（读）；[src/drivers/FlowPulseReader.cpp:53–61](src/drivers/FlowPulseReader.cpp:53)（ISR 写）
- **现状**：ESP32 单条 32 位读对单调递增计数器在实践中可接受。
- **隐患**：违反 volatile/atomic 最佳实践；编译器升级或目标芯片变化时可能撕裂。
- **修复方向**：将 `dropped_` 改为 `std::atomic<std::uint32_t>` 或读取时关中断。

#### C-03 `WaterController::checkTarget` 对 `preset->value==0` 无显式守卫 — 低
- **位置**：[src/app/WaterController.cpp:247–261](src/app/WaterController.cpp:247)
- **现状**：sanitizeConfig 已 clamp 容量预设到 ≥100ml、时间预设到 ≥5s；`selectedPresetConfig()` 仅返回 enabled 项；不可触发。
- **隐患**：直接绕过 ConfigStore 写入异常 NVS 的极端情形会让"按 OK 即完成"。
- **修复方向**：`if (preset->value == 0) return false;` 作为防御性断言。

#### C-04 `initializeApplication` 中 `new` 分配无失败检查、config 加载失败无降级日志 — 中
- **位置**：[src/main.cpp:177–207](src/main.cpp:177)
- **现状**：`g_filters/g_app/g_display = new ...` 后续直接使用；`g_config = g_configStore.loadSystemConfig()` 在底层失败时静默回落到默认配置（参见 [src/app/ConfigStore.cpp:165–167](src/app/ConfigStore.cpp:165)）但不打印警告日志。
- **隐患**：嵌入式 OOM 概率低但非零；config 静默重置违反 AGENTS.md "不能因固件升级静默清空用户配置"。
- **修复方向**：检查每个 `new` 返回非空；为"未识别版本号 / NVS 读取失败"打 ESP32BASE_LOG_W；考虑用 placement new 或栈上对象避免堆分配。

#### C-05 全局裸指针生命周期管理薄弱 — 低
- **位置**：[src/main.cpp:184–186](src/main.cpp:184)；[src/main.cpp:209–212](src/main.cpp:209) 的 `if (!g_app) return`
- **现状**：嵌入式单实例可接受；setup() 失败后 loop() 软跳过。
- **隐患**：初始化中途 abort 时设备进入 watchdog 重启循环；没有诊断。
- **修复方向**：使用 `static` 实例或 `std::optional<T>`/`std::aligned_storage`，并在初始化失败时记录原因后主动重启。

#### C-06 工厂复位组合键 `comboEmitted_` 重置后可重复触发 — 中
- **位置**：[src/app/ButtonInput.cpp:72–89](src/app/ButtonInput.cpp:72)；上层消费在 [src/app/AppController.cpp:132–137](src/app/AppController.cpp:132)
- **现状**：长按 STOP+OK 5 秒触发 `FactoryResetCombo` 后，松手任一键 `comboArmed_/comboEmitted_` 清零；再次同时按住又会再次触发。AppController 行 132 设置 `localMode_ = FactoryResetConfirm`，第二次触发会重新进入确认态，期间 `factoryConfirmArmedMs_` 也被重置。
- **隐患**：用户抖动或反复按住可触发多次确认，确认窗口被刷新；测试或维护时不慎多按可意外完成工厂复位。
- **修复方向**：在 ButtonInput 中加锁，要求两键完全松开 ≥X ms 后才允许下一次 combo；或在 AppController 中加冷却。

#### C-07 FlowPulseReader 丢包仅静默累计 — 中
- **位置**：[src/drivers/FlowPulseReader.cpp:53–61](src/drivers/FlowPulseReader.cpp:53)
- **现状**：环形缓冲容量固定 32（`pulseTimes_[32]`）；满则 `++dropped_`。`FlowMeter::snapshot` 也暴露 `rejectedPulses_` 字段，但未发现路径将 `dropped_` 上报到统计/日志/Web。
- **隐患**：高流量场景或主循环抖动下脉冲被丢，水量计量偏低，用户和运维都无感知。
- **修复方向**：在 AppController 周期检查 `g_flowPulses.droppedPulses()` 增量并写入 ESP32BASE_LOG_W；或在 Web `/api/faucet/status` 中暴露字段。

#### C-08 `millis()` 49 天回绕处理不一致 — 低
- **位置**：多处，例如 [src/app/ButtonInput.cpp:67](src/app/ButtonInput.cpp:67)、[src/app/AppController.cpp:48–50](src/app/AppController.cpp:48)、[src/app/AppController.cpp:144](src/app/AppController.cpp:144)
- **现状**：使用 `nowMs >= startMs ? nowMs - startMs : 0`，回绕瞬间等同重新计时。也存在 `nowMs + 300UL` 前向加值后再比较（line 144），加值后回绕可能产生反向比较错误。
- **隐患**：连续运行 49 天的设备在跨越翻转点的瞬间，长按窗口或防抖窗口可能被丢失或被错误延长。
- **修复方向**：统一改用 `std::int32_t` 差值（带符号减法 + 比较 `>= 0`）或 64 位 `esp_timer_get_time()`。短期文档化"49 天重启策略"亦可。

---

### 2.2 代码 — Web/配置/持久化层

#### W-01 Web 表单 checkbox 无法关闭（实际可复现 bug）— 高
- **位置**：
  - 预设 enabled：[src/web/FaucetWeb.cpp:887–891](src/web/FaucetWeb.cpp:887)（`handlePresetsApi`）
  - 全局 beepEnabled：[src/web/FaucetWeb.cpp:859](src/web/FaucetWeb.cpp:859) → [src/web/FaucetWeb.cpp:820–826](src/web/FaucetWeb.cpp:820)（`applyBoolParam`）
  - 表单中 checkbox 只在勾选时发送 `enabled=1`：[src/web/FaucetWeb.cpp:307–312](src/web/FaucetWeb.cpp:307)（`sendCheckbox`）
- **现状**：`applyBoolParam` 与 `handlePresetsApi` 中的 `getParam("enabled", ...)` 在表单未发送该字段时**直接跳过赋值**，原值保留。HTML 表单的 unchecked checkbox 不会发送任何键值对，因此用户取消勾选并保存后，`preset.enabled` / `config.beepEnabled` 永远不会被改为 false。
- **对照**：滤芯路径 [src/web/FaucetWeb.cpp:962](src/web/FaucetWeb.cpp:962) 使用 `Esp32BaseWeb::hasParam("enabled")` 直接将"未发送"映射为 false，正确。
- **影响**：用户无法通过 Web 关闭任意预设、无法关闭蜂鸣器；只能在前端通过工程师/JS 手段强制提交 `enabled=0` 才能实现。AGENTS.md 规定"用户持久化配置必须保护"，此 bug 让"取消"指令丢失，构成数据/UX 缺陷。
- **修复方向**：将所有 checkbox 字段改用 `enabled = Esp32BaseWeb::hasParam("enabled")` 模式（与滤芯一致）；或在表单里追加隐藏字段 `<input type='hidden' name='enabled' value='0'>` 配合显式 `applyBoolParam` 覆盖。

#### W-02 HTML 属性中输出用户名称仅依赖 `writeHtmlEscaped` — 中
- **位置**：
  - 预设名称在 `value='...'` 单引号属性内：[src/web/FaucetWeb.cpp:440–442](src/web/FaucetWeb.cpp:440)
  - 滤芯名称在 `value='...'` 单引号属性内：[src/web/FaucetWeb.cpp:613–615](src/web/FaucetWeb.cpp:613)
- **现状**：`Esp32BaseWeb::writeHtmlEscaped` 实现位于基础库，未在本项目中确认是否转义单引号 `'`。
- **隐患**：若 `writeHtmlEscaped` 仅转义 `<>&"`，名称中包含单引号的用户输入可闭合属性、注入 `onerror=` 等。预设名称限长 15、滤芯限长 15 加上预设/滤芯路径需要认证后才能写入降低了风险，但不能依赖。
- **修复方向**：把所有用户字符串改写到 `<input value="...">`（双引号）配合 `&quot;` 转义，或在基础库中提供 `writeHtmlAttrEscaped`。同步在 `docs/base-library-requests/` 提交一篇请求。

#### W-03 32KB 日志 JSON + 静态记录数组占用显著 RAM — 中
- **位置**：[src/web/FaucetWeb.cpp:925–926](src/web/FaucetWeb.cpp:925) `static WaterLogRecord records[kMaxLogPageSize]; static char json[32768];`
- **现状**：静态分配，规避了请求中的栈/堆压力，但常驻 RAM。
- **隐患**：ESP32 RAM 约 320KB（包含 IRAM/DRAM 划分），Web、WiFi、TLS、其它静态缓冲叠加后可用 heap 紧张；日志接口并发只有一个，但静态全局占用会减少 OTA 任务、Esp32Base FILELOG 等的余量。
- **修复方向**：缩小到 8–16KB 配合分页限制 `kMaxLogPageSize=50`；或改为按需 `malloc/free`；或流式 chunked 输出避免大缓冲。

#### W-04 Web handler 与 AppController/ConfigStore 之间的并发模型不显式 — 中
- **位置**：[src/web/FaucetWeb.cpp:828–835](src/web/FaucetWeb.cpp:828) `saveConfigAndReply` 直接修改 `*g_context.config` 并写 NVS；主循环在 [src/main.cpp:209](src/main.cpp:209) 之后读取 `g_config`
- **现状**：Esp32Base Web 任务与 Arduino loop 通常分别运行；代码未见 mutex/portENTER_CRITICAL。
- **隐患**：表单保存瞬间 AppController::tick 读到部分写入的 config 字段（uint32_t 与 float 多字段），可能短暂使用混合状态计算阀控/统计。`sanitizeConfig` 也是非原子。
- **修复方向**：把 g_config 修改路径整合为"复制 → 修改 → 提交"原子拷贝（先拷贝快照，修改完整后通过 mutex/critical section 切换指针）；或在 ConfigStore 中提供 `applyConfigUpdate(callback)` 封装。

#### W-05 LittleFS 容量监控缺失 — 中
- **位置**：[partitions_4mb.csv:7](partitions_4mb.csv:7) `spiffs ... 0x120000` (1152 KB)；[src/app/WaterLogFileStore.cpp](src/app/WaterLogFileStore.cpp)
- **现状**：日志按容量回滚，不会写满崩溃；但配置/统计/滤芯/未来新增数据无统一额度监控。
- **隐患**：长期运行后剩余空间逼近 0，无告警；新增功能时容易踩坑。
- **修复方向**：开机和每日检查 LittleFS 剩余容量，低于阈值（如 100KB）时 ESP32BASE_LOG_W；Web 状态接口暴露。

#### W-06 NVS 中 `ver` 大于 `kConfigVersion`（降级固件）静默回落默认 — 中
- **位置**：[src/app/ConfigStore.cpp:62–64](src/app/ConfigStore.cpp:62) `isKnownSystemConfigVersion`；[src/app/ConfigStore.cpp:165–167](src/app/ConfigStore.cpp:165) "未知版本→默认配置"
- **现状**：低版本固件读取高版本写入的 NVS 时，`isKnownSystemConfigVersion` 返回 false，加载阶段直接返回默认配置；此后任意保存会用默认值覆盖 NVS。
- **隐患**：从新固件回滚到旧固件时用户配置被静默丢失，违反 AGENTS.md "用户持久化配置必须保护"。
- **修复方向**：识别到未来版本时不要清空，转而进入只读模式 + 明确日志/Web 告警；要求用户手动重置才允许覆盖。

#### W-07 出水控制黑名单使用子串匹配 — 低
- **位置**：[src/web/FaucetWebRoutes.cpp:45–57](src/web/FaucetWebRoutes.cpp:45) `faucetWebRouteAllowed`
- **现状**：当前所有 `kRoutes` 均不包含黑名单路径；`registerFaucetWeb`（[src/web/FaucetWeb.cpp:1099–1120](src/web/FaucetWeb.cpp:1099)）也会检查；双层防御。
- **隐患**：未来新增路径如 `/api/faucet/water/log` 之类无害名字会被误判禁止；黑名单 + 子串匹配长期不如白名单严格。
- **修复方向**：改为白名单（kRoutes 即白名单），删除 `faucetWebRouteAllowed` 的子串黑名单或仅保留作为单元测试用约束。

---

### 2.3 文档质量与一致性

#### D-01 配置版本化机制在 docs/ 中无设计说明 — 中
- **位置**：[docs/03-software-architecture.md](docs/03-software-architecture.md) 提及"配置版本化"但无版本号、迁移规则、回滚策略；实际实现见 [src/app/ConfigStore.cpp:14](src/app/ConfigStore.cpp:14) 的 `kConfigVersion = 3` 与 `loadLegacyCalibrationTarget/loadLegacyFilters`。
- **隐患**：新人或回归测试无法快速验证升级路径；W-06 这类"降级回落默认"的设计选择缺乏文档支撑。
- **修复方向**：在 docs/03 增设"持久化版本化"小节，列出每个版本的字段差异、迁移规则、降级策略、未识别版本的处理。

#### D-02 docs/04 本地菜单与代码实际能力对照模糊 — 低
- **位置**：[docs/04-ui-interaction.md](docs/04-ui-interaction.md) 中的本地菜单描述（长按 OK 进入校准等）；代码实际行为见 [src/app/AppController.cpp:124–141](src/app/AppController.cpp:124)
- **隐患**：长按 NEXT 是否进入额外菜单、菜单层级、退出方式未在文档中明确；阅读者难以判断哪些菜单已实现/未实现。
- **修复方向**：在 docs/04 中加入"已实现/未实现"小节并打对应代码行号链接。

#### D-03 old-docs 与新 docs 矛盾点未在每篇旧文档头部标注 — 低
- **位置**：[old-docs/](old-docs/) 全部
- **隐患**：旧版含 EC11 编码器、AsyncWebServer 等已废弃方案；尽管 AGENTS.md 已声明 old-docs 仅作背景，但 LLM/新人仍可能误读。
- **修复方向**：在每篇旧文档顶部加一行 "> 本文档为历史背景，最新设计见 docs/0X-XXX.md"。

#### D-04 docs/06 任务进度未更新到当前已完成状态 — 低
- **位置**：[docs/06-implementation-plan.md](docs/06-implementation-plan.md)；当前 README 已声明"主固件已接入 ... 裸板验证已通过"
- **隐患**：实施任务书内的勾选状态过期，新参与者难以判断哪些任务真正待办。
- **修复方向**：标注每节"已完成 / 进行中 / 待办"，或在文档末尾加版本表与最近一次同步日期。

#### D-05 docs/base-library-requests/ 中各请求项未追踪状态 — 低
- **位置**：[docs/base-library-requests/](docs/base-library-requests/) 下 5 篇请求文档
- **隐患**：哪些已被 Esp32Base 接受/合入、哪些待回应不清晰；未来重复提交概率上升。
- **修复方向**：在 README.md（同目录）维护一张状态表（题目、状态、关联 Esp32Base commit/PR）。

---

### 2.4 测试覆盖与质量

#### T-01 缺少配置版本升级路径专项测试 — 高
- **位置**：[test/native/test_config_store/test_config_store.cpp](test/native/test_config_store/test_config_store.cpp)
- **现状**：现有用例覆盖默认值、范围 clamp、预设/滤芯保存读取，但未模拟 NVS 中 ver=1/2/未来值/损坏数据加载并验证迁移结果。
- **隐患**：与 W-06 直接相关；版本演进时回归保护不足。
- **修复方向**：用 FakeConfigBackend 预置 v1/v2/v999 键值，断言 loadSystemConfig 后字段正确升级 + saveSystemConfig 回写为最新版本。

#### T-02 WaterController 缺少硬件故障端到端断言 — 高
- **位置**：[test/native/test_water_controller/test_water_controller.cpp](test/native/test_water_controller/test_water_controller.cpp)
- **现状**：覆盖正常预设完成、确认超时、用户停止、暂停恢复；未明显覆盖 noFlowTimeout、highFlow 持续保护、maxOutTime 强制断电的"安全停止"路径。
- **修复方向**：补充三类断言：(1) Running 状态下 0 流量持续 noFlowTimeoutSec，期望 SafetyStopped；(2) 流量持续超 highFlowMlPerMin 达到 highFlowDurationSec，期望 SafetyStopped；(3) 累计时长超 maxOutTimeSec，期望 SafetyStopped。

#### T-03 FlowMeter 缺少高频脉冲与浮点边界测试 — 中
- **位置**：[test/native/test_flow_meter/test_flow_meter.cpp](test/native/test_flow_meter/test_flow_meter.cpp)
- **现状**：覆盖基础脉冲累计与流量换算。
- **修复方向**：加入 (1) `pulsePerMl=0.05/5.0` 的边界精度断言；(2) 短间隔脉冲（接近 1 µs）下 `flowFromInterval` 不会溢出 uint32_t；(3) 大量脉冲累计 (>百万) 下 `volumeFromPulses` 与 ml→L 转换正确。

#### T-04 CalibrationController 缺少取消重启与多目标循环测试 — 中
- **位置**：[test/native/test_calibration_controller/test_calibration_controller.cpp](test/native/test_calibration_controller/test_calibration_controller.cpp)
- **修复方向**：加入 (1) Sampling 中 reset，验证状态、samples、proposed 均回到初值；(2) 连续在多个 target 上各采样多次，验证 samples 对应 target 隔离。

#### T-05 ButtonInput 长按竞态、组合键序列、消抖边界缺乏覆盖 — 中
- **位置**：[test/native/test_button_input/test_button_input.cpp](test/native/test_button_input/test_button_input.cpp)
- **修复方向**：参数化时间戳到边界值（kButtonDebounceMs±1）、测试 STOP+OK 触发后再连续按住的去抖（与 C-06 相关）、加入"NEXT 与 OK 短按近乎同时"的优先级断言。

#### T-06 DisplayPresenter 测试薄弱 — 中
- **位置**：[test/native/test_display_beep/test_display_beep.cpp](test/native/test_display_beep/test_display_beep.cpp)
- **修复方向**：拆分为 `test_display_presenter` 与 `test_beep_driver`；针对页面切换、空闲熄屏、按键唤醒、超时回首页等转移补断言。

#### T-07 AppController 顶层集成测试链路覆盖不足 — 高
- **位置**：[test/native/test_app_controller/test_app_controller.cpp](test/native/test_app_controller/test_app_controller.cpp)
- **现状**：247 行，未明显覆盖完整业务流（开始→暂停→恢复→完成→记录写入→统计累计→滤芯耗用 → Web 快照展示）。
- **修复方向**：补充一组"端到端剧本"测试，模拟 ButtonLevels 序列 + 流量脉冲 + 时间推进，断言最终 `snapshot()` 与 logs/statistics/filters 均符合预期。

#### T-08 Web 路由与 JSON 反序列化的恶意/畸形输入未明显覆盖 — 中
- **位置**：[test/native/test_faucet_web_routes/](test/native/test_faucet_web_routes/test_faucet_web_routes.cpp)、[test/native/test_faucet_web_json/](test/native/test_faucet_web_json/test_faucet_web_json.cpp)
- **修复方向**：给 `parseU32`/`parseDate`/`parseFloat`/`parseLitersToMl` 加超长字符串、负数、非数字尾部、`NaN`/`Inf`/`1e9999` 等输入；断言均返回 false 不修改原值。

---

### 2.5 工程与构建

#### E-01 platformio.ini 含开发者机器绝对路径 — 中
- **位置**：[platformio.ini:11](platformio.ini:11) `extra_scripts = post:/Users/tyg/dir/claude_dir/Esp32Base/scripts/esp32base_webota.py`；[platformio.ini:29](platformio.ini:29) `symlink:///Users/tyg/dir/claude_dir/Esp32Base`
- **隐患**：其它机器无法直接 `pio run`；CI 不可用。
- **修复方向**：定义 `${env.ESP32BASE_PATH}` 环境变量或 platformio `[platformio] extra_configs` 引用本地 override 文件；README 文档化所需变量。

#### E-02 webota 默认凭据 admin/admin 明文入库 — 中
- **位置**：[platformio.ini:13–14](platformio.ini:13)
- **隐患**：默认凭据若被复制到生产固件 + 被用户直接使用，远端 OTA 入口风险高；密码以明文随仓库分发。
- **修复方向**：把 host/user/password 迁出到 `platformio.ini.local`（被 .gitignore），仓库仅保留示例 `platformio.ini.example`；README 说明部署前必须改默认值。

#### E-03 .gitignore 极简 — 低
- **位置**：[.gitignore](.gitignore) 仅 4 行
- **修复方向**：补充 `.env*`、`build/`、`*.swp`、`*.swo`、`*.bak`、`platformio.ini.local`、`data/runtime/` 等常见模式。

#### E-04 无 CI 配置 — 低
- **位置**：项目根
- **修复方向**：考虑增设 `.github/workflows/native.yml`，至少跑 `pio test -e native` 和 `pio run -e esp32dev` 编译。

#### E-05 缺少 LICENSE — 低
- **位置**：项目根
- **修复方向**：选择 MIT/Apache-2.0 等并补充。

---

## 3. 不列为问题的核实结果（透明度声明）

代理初版报告中以下结论经人工核实**不成立或已被现有实现覆盖**：

- **FlowPulseReader::pop() 临界区问题**：[src/drivers/FlowPulseReader.cpp:18–28](src/drivers/FlowPulseReader.cpp:18) `pulseTimes_[tail_]` 的读取已在 `noInterrupts/interrupts` 临界区内，无撕裂风险。
- **FlowMeter pulsePerMl=0 / WaterController preset->value=0 触发**：被 `sanitizeConfig` clamp 与 `selectedPresetConfig()` enabled 检查双重挡住，实际不可触发。降为 C-01/C-03 防御性建议。
- **ConfigStore 版本迁移**：v1→v3 自动升级 + 自动回写，符合 AGENTS.md 配置保护；只剩 W-06 描述的"未来版本降级"未防护。
- **Web 出水控制限制**：[src/web/FaucetWebRoutes.cpp:8–25](src/web/FaucetWebRoutes.cpp:8) 路由表 + [src/web/FaucetWebJson.cpp:111–120](src/web/FaucetWebJson.cpp:111) 状态接口的 `"waterControl":false` 字段双重表达，符合 AGENTS.md "Web 不得提供任何远程出水控制能力"。
- **JSON 字符串转义**：[src/web/FaucetWebJson.cpp:95–105](src/web/FaucetWebJson.cpp:95) `appendEscaped` 处理引号、反斜杠、控制字符，过滤 0x00–0x1F；JSON 注入面闭合。
- **认证全覆盖**：每个 Page 与 API handler 均以 `Esp32BaseWeb::checkAuth()` 起手。
- **HTTP 方法语义合理**：GET 读、POST 写、ANY 用于复合（与表单一致）。
- **路由数量**：`faucetWebRoutesFitEsp32Base()` 在注册时校验，安全网到位。

---

## 4. 整体结论与修复优先级

**整体质量良好**：分层清晰（drivers/app/web）、配置版本化扎实、Web 不允许远程出水的安全约束有"代码白名单 + 黑名单 + JSON 字段 + 路由测试"四重表达；AGENTS.md 项目原则在代码中得到执行。

**建议优先级**：

| 阶段 | 项目 |
|---|---|
| 立即修复（高） | W-01（checkbox 失效）、T-01（配置升级测试）、T-02（硬件故障测试）、T-07（端到端集成测试） |
| 近期修复（中） | C-04 / C-06 / C-07、W-02 / W-03 / W-04 / W-05 / W-06、D-01、E-01 / E-02 |
| 长期演进（低） | C-01 / C-02 / C-03 / C-05 / C-08、W-07、其余文档与工程项 |

**说明**：本文档仅评估，不动手修。如需按此清单逐项推进，建议为每条创建独立 issue/任务并参考"修复方向"作为起点。
